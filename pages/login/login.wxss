/* 登录页 — v1.0.3 统一设计系统 */
.page {
  min-height: 100vh;
  background: linear-gradient(180deg, var(--color-surface-warm) 0%, var(--color-paper-dark) 40%, var(--color-surface) 100%);
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 0 48rpx;
}

/* ========================================
   品牌区
   ======================================== */
.brand {
  text-align: center;
  padding-top: 100rpx;
  padding-bottom: 48rpx;
}

.brand__deco {
  font-size: 28rpx;
  color: var(--color-text-light);
  letter-spacing: 12rpx;
  margin-bottom: 32rpx;
  opacity: 0.7;
}

.brand__icon {
  font-size: 88rpx;
  display: block;
  margin-bottom: 20rpx;
  filter: drop-shadow(0 4rpx 8rpx rgba(var(--color-primary-rgb), 0.1));
}

.brand__title {
  font-size: 48rpx;
  font-weight: 700;
  color: var(--color-primary);
  display: block;
  letter-spacing: 8rpx;
  text-shadow: 0 2rpx 0 rgba(var(--color-primary-rgb), 0.08);
}

.brand__desc {
  font-size: 24rpx;
  color: var(--color-text-light);
  margin-top: 14rpx;
  display: block;
  letter-spacing: 4rpx;
}

/* ========================================
   登录面板
   ======================================== */
.login-panel {
  width: 100%;
  display: flex;
  flex-direction: column;
  align-items: center;
}

/* ========================================
   头像选择卡片 — 横向布局
   ======================================== */
.profile-card {
  width: 100%;
  background: var(--color-white);
  border-radius: 24rpx;
  padding: 36rpx 40rpx;
  box-shadow: 0 4rpx 24rpx rgba(var(--color-primary-rgb), 0.06);
  display: flex;
  flex-direction: row;
  align-items: center;
  margin-bottom: 40rpx;
  box-sizing: border-box;
}

.profile-card__left {
  flex-shrink: 0;
  margin-right: 36rpx;
}

.profile-card__right {
  flex: 1;
  min-width: 0;
  display: flex;
  flex-direction: column;
  justify-content: center;
}

.avatar-btn {
  width: 130rpx;
  height: 130rpx;
  border-radius: 50%;
  padding: 0;
  margin: 0;
  background: transparent;
  border: none;
  line-height: normal;
  position: relative;
  display: block;
}

.avatar-btn::after {
  border: none;
}

.avatar-btn__img {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  border: 3rpx solid var(--color-border);
  box-shadow: 0 4rpx 16rpx rgba(var(--color-primary-rgb), 0.1);
}

.avatar-btn__empty {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--color-surface) 0%, var(--color-surface) 100%);
  border: 3rpx dashed var(--color-text-light);
  display: flex;
  align-items: center;
  justify-content: center;
  box-sizing: border-box;
  transition: border-color 0.2s;
}

.avatar-btn__icon {
  font-size: 40rpx;
}

/* 头像右下角相机角标 */
.avatar-btn__badge {
  position: absolute;
  right: 0;
  bottom: 0;
  width: 44rpx;
  height: 44rpx;
  border-radius: 50%;
  background: var(--color-primary);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 22rpx;
  box-shadow: 0 2rpx 8rpx rgba(var(--color-primary-rgb), 0.25);
}

/* 昵称输入框 */
.nickname-input {
  width: 100%;
  height: 88rpx;
  padding: 0 28rpx;
  border-radius: 16rpx;
  background: var(--color-paper);
  border: 2rpx solid var(--color-border);
  font-size: 30rpx;
  color: var(--color-text);
  text-align: center;
  box-sizing: border-box;
  transition: border-color 0.2s;
}

.nickname-input:focus {
  border-color: var(--color-primary);
  background: var(--color-white);
  box-shadow: 0 0 0 6rpx rgba(var(--color-primary-rgb), 0.1);
}

.nickname-input::placeholder {
  color: var(--color-text-light);
}

/* 隐藏昵称输入框（不可见但可聚焦以触发微信昵称获取） */
.nickname-input--hidden {
  position: absolute;
  left: -9999rpx;
  width: 1rpx;
  height: 1rpx;
  opacity: 0;
  padding: 0;
  border: none;
  pointer-events: none;
}

/* 昵称展示区（横向卡片右半部） */
.nickname-display {
  width: 100%;
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 8rpx 0;
  border-radius: 12rpx;
  min-height: 80rpx;
}

.nickname-display--waiting {
  border: 2rpx dashed var(--color-border);
  padding: 16rpx 20rpx;
}

.nickname-display__icon {
  font-size: 22rpx;
}

.nickname-display__text {
  font-size: 30rpx;
  color: var(--color-text);
  font-weight: 600;
  line-height: 1.4;
}

.nickname-display__hint {
  font-size: 22rpx;
  color: var(--color-success);
  margin-top: 4rpx;
}

.nickname-display--waiting .nickname-display__text {
  color: var(--color-text-light);
  font-weight: 400;
  font-size: 28rpx;
}

/* ========================================
   微信登录按钮
   ======================================== */
.wechat-btn {
  width: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8rpx;
  padding: 28rpx 0;
  border-radius: 20rpx;
  font-size: 34rpx;
  font-weight: 600;
  border: none;
  background: linear-gradient(135deg, var(--color-primary-light) 0%, var(--color-primary-dark) 100%);
  color: var(--color-white);
  letter-spacing: 4rpx;
  box-shadow: 0 4rpx 20rpx rgba(var(--color-primary-rgb), 0.25);
  transition: all 0.15s;
}

.wechat-btn:active {
  transform: scale(0.97);
  opacity: 0.92;
  box-shadow: 0 2rpx 12rpx rgba(var(--color-primary-rgb), 0.15);
}

.wechat-btn--dark {
  background: linear-gradient(135deg, var(--color-primary-dark) 0%, var(--color-primary-dark) 100%);
  box-shadow: 0 4rpx 20rpx rgba(107, 76, 59, 0.2);
}

.wechat-btn__arrow {
  font-size: 32rpx;
  font-weight: 400;
}

.login-hint {
  display: block;
  text-align: center;
  font-size: 24rpx;
  color: var(--color-text-light);
  margin-top: 16rpx;
}

.login-hint--ready {
  color: var(--color-primary);
  font-weight: 500;
}

/* ========================================
   其他登录方式
   ======================================== */
.alt-login {
  width: 100%;
  margin-top: 48rpx;
}

.alt-login__divider {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-bottom: 24rpx;
}

.alt-login__line {
  flex: 1;
  height: 1rpx;
  background: var(--color-border);
}

.alt-login__label {
  font-size: 24rpx;
  color: var(--color-text-light);
  white-space: nowrap;
}

.alt-login__back {
  font-size: 24rpx;
  color: var(--color-primary);
  white-space: nowrap;
}

.alt-login__links {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 32rpx;
}

.alt-login__link {
  font-size: 28rpx;
  color: var(--color-primary);
  padding: 8rpx 0;
}

.alt-login__link--active {
  color: var(--color-primary);
  font-weight: 600;
}

.alt-login__sep {
  font-size: 24rpx;
  color: var(--color-border);
}

/* ========================================
   表单（邮箱/手机号）
   ======================================== */
.form-group {
  width: 100%;
  margin-bottom: 24rpx;
}

.form-label {
  font-size: 26rpx;
  color: var(--color-text);
  display: block;
  margin-bottom: 10rpx;
  padding-left: 4rpx;
}

.form-input {
  width: 100%;
  height: 88rpx;
  padding: 0 28rpx;
  border-radius: 16rpx;
  background: var(--color-white);
  border: 2rpx solid var(--color-border);
  font-size: 30rpx;
  color: var(--color-text);
  box-sizing: border-box;
  transition: border-color 0.2s, box-shadow 0.2s;
}

.form-input:focus {
  border-color: var(--color-primary);
  box-shadow: 0 0 0 6rpx rgba(var(--color-primary-rgb), 0.1);
}

/* ========================================
   已登录状态
   ======================================== */
.logged-in-card {
  text-align: center;
  padding: 48rpx 40rpx;
  background: var(--color-white);
  border-radius: 24rpx;
  margin-bottom: 24rpx;
  width: 100%;
  box-sizing: border-box;
  box-shadow: 0 4rpx 24rpx rgba(var(--color-primary-rgb), 0.06);
}

.logged-in__avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  margin-bottom: 16rpx;
  border: 3rpx solid var(--color-border);
  box-shadow: 0 4rpx 12rpx rgba(var(--color-primary-rgb), 0.08);
}

.logged-in__avatar--placeholder {
  background: var(--color-surface);
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 56rpx;
  margin-left: auto;
  margin-right: auto;
}

.logged-in__title {
  font-size: 34rpx;
  font-weight: 600;
  color: var(--color-text);
  display: block;
}

.logged-in__user {
  font-size: 26rpx;
  color: var(--color-primary);
  display: block;
  margin: 8rpx 0 24rpx 0;
}

.logged-in__actions {
  display: flex;
  justify-content: center;
  gap: 16rpx;
}

.login-divider {
  display: flex;
  align-items: center;
  gap: 16rpx;
  margin-top: 32rpx;
  width: 100%;
}

.login-divider::before,
.login-divider::after {
  content: '';
  flex: 1;
  height: 1rpx;
  background: var(--color-border);
}

.login-divider text {
  font-size: 24rpx;
  color: var(--color-text-light);
}

/* ========================================
   跳过
   ======================================== */
.skip-section {
  text-align: center;
  margin-top: auto;
  padding-top: 24rpx;
  padding-bottom: calc(64rpx + env(safe-area-inset-bottom));
}

.skip-link {
  font-size: 28rpx;
  color: var(--color-primary);
  display: inline-block;
  padding: 16rpx 40rpx;
  border-radius: 40rpx;
  background: rgba(255, 255, 255, 0.5);
  border: 2rpx solid var(--color-border);
  transition: all 0.15s;
}

.skip-link:active {
  background: rgba(255, 255, 255, 0.8);
  border-color: var(--color-primary-light);
  color: var(--color-text-secondary);
}
